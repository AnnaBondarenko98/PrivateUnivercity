@model Anna_Bondarenko_FinalTask.Models.Models.ExaminationSubject

@{
    ViewBag.Title = "ExaminationSubjectDelete";
}

@Html.ValidationMessage("ExSubjectDelete", new { @class = "text-danger" })

    <h3>Are you sure you want to delete this?</h3>
    <div class="col-lg-11 centered">
        <dl class="dl-horizontal font-details">
            <dt>
                @Html.DisplayNameFor(model => model.Name)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.Name)
            </dd>

        </dl>

        @using (Html.BeginForm()) {
            @Html.AntiForgeryToken()

            <div class="">
                <input type="submit" value="Delete" class="btn btn-default" /> 
                <div class="btn btn-partial">@Html.ActionLink("Back to List of Examination Subjects", "GetExaminationSubject", new{area="Operator", controller="ExaminationSubject"})</div>
            </div>
        }
    </div>


